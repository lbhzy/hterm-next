[project]
name = "hterm-next"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pyside6>=6.10.1",
    "pyte",
    "qtawesome>=1.4.0",
    "toml>=0.10.2",
]

[tool.uv.sources]
pyte = { git = "https://github.com/selectel/pyte.git" }
